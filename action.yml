name: "Path lister action"
description: "List specific file paths in project"
author: "Rishabh Raizada"
# inputs:
#   path:
#     description: "Path to the YAML file to be linted"
#     required: true
#   strict:
#     description: "Run the linter in strict mode (error on warnings)"
#     required: false
#     default: false
outputs:
  paths:
    description: "Paths of all files with given extension"
runs:
  using: "docker"
  image: "Dockerfile"
branding:
  icon: "list"
  color: "red"
